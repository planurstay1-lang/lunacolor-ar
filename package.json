{
  "name": "lunacolor-ar",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173 --strictPort",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "VITE_BASE=/lunacolor-ar/ vite build && touch dist/.nojekyll && cd dist && git init -q -b gh-pages && git add -A && git commit -q -m deploy && git push -f https://github.com/planurstay1-lang/lunacolor-ar.git gh-pages && cd .. && rm -rf dist/.git"
  },
  "dependencies": {
    "@8thwall/engine-binary": "^1.0.0",
    "qrcode": "^1.5.4",
    "three": "^0.166.1"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "vite": "^5.4.21"
  }
}
